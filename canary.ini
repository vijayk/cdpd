[impala]
COMPONENT_NAME = "impala"
    [[text-replace]]
        REPLACE_6 = "__REGISTRY__", "docker-sandbox.infra.cloudera.com/cdh/", cloudera/docker_images.yml, regex_replace

[hue]
build_tool = maven

    [[install_cmd]]
       cmd_0 = bash -c "GBN=${STACKVERSION}-${BUILDNUMBER}-${GBN} bash ./tools/container/build.sh"
