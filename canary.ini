[impala]
COMPONENT_NAME = "impala"
    [[text-replace]]
        REPLACE_6 = "__REGISTRY__", "docker-sandbox.infra.cloudera.com/cdh/", cloudera/docker_images.yml, regex_replace

[hue]
build_tool = maven
DOCKER_REGISTRY = "docker-sandbox.infra.cloudera.com/cdh"

    [[install_cmd]]
       cmd_0 = bash -c "REGISTRY=${DOCKER_REGISTRY} GBN=${STACKVERSION}-${BUILDNUMBER}-${CANARY_ID} bash ./tools/container/build.sh"

    [[text-replace]]
        replace_1 = "__REGISTRY__", ${DOCKER_REGISTRY}, cloudera/docker_images.yaml, regex_replace
